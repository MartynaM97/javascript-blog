const tags = ["tag1", "tag2", "tag3"];

for (let tag in tags) {
  console.log(tags[tag]);
}

for (let i = 0; i < tags.length; i++) {
  console.log(tags[i]);
}

const obj = {
  name: "Martyna",
  age: 25,
};

obj.

